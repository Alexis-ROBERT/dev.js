import CommandLineInterfaceConstructor from '../sources/cli.constructor';

export default class CreateProject extends CommandLineInterfaceConstructor {
        public constructor() {
                super('create-project');
        }
}
