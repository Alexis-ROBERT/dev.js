import CommandLineInterfaceConstructor from '../../sources/cli.constructor';

export default class EmberServer extends CommandLineInterfaceConstructor {
        public constructor() {
                super('server');
        }
}
