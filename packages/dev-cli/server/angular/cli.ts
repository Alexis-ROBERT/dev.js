import CommandLineInterfaceConstructor from '../../sources/cli.constructor';

export default class AngularServer extends CommandLineInterfaceConstructor {
        public constructor() {
                super('server');
        }
}
