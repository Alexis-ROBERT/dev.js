import CommandLineInterfaceConstructor from '../../sources/cli.constructor';

export default class VueServer extends CommandLineInterfaceConstructor {
        public constructor() {
                super('server');
        }
}
