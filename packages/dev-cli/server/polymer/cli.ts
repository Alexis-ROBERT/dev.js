import CommandLineInterfaceConstructor from "../../sources/cli.constructor";

export default class PolymerServer extends CommandLineInterfaceConstructor {
        public constructor() {
                super('server');
        }
}