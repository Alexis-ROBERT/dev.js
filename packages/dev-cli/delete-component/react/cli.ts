import CommandLineInterfaceConstructor from '../../sources/cli.constructor';

export default class ReactDeleteComponent extends CommandLineInterfaceConstructor {
        public constructor() {
                super('delete-component');
        }
}
